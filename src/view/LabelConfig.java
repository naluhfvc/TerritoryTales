package view;

public class LabelConfig {
    int x, y, width, height;

    LabelConfig(int x, int y, int width, int height) {
        this.x = x;
        this.y = y;
        this.width = width;
        this.height = height;
    }
}
